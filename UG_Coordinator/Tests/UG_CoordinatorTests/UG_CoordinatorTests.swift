    import XCTest
    @testable import UG_Coordinator

    final class UG_CoordinatorTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(UG_Coordinator().text, "Hello, World!")
        }
    }
